INCLUDE(inc.cmake)

IF(OS_CONTRIB_ZLIB_SRC)
	ADD_LIBRARY(zlib_static STATIC ${OS_CONTRIB_ZLIB_SRC})
	SET_TARGET_PROPERTIES(zlib_static PROPERTIES OUTPUT_NAME z)
	ADD_LIBRARY(zlib SHARED ${OS_CONTRIB_ZLIB_SRC})
	SET_TARGET_PROPERTIES(zlib PROPERTIES OUTPUT_NAME z)
	SET_TARGET_PROPERTIES(zlib PROPERTIES CLEAN_DIRECT_OUTPUT 1)
	SET_TARGET_PROPERTIES(zlib PROPERTIES SOVERSION ${VERSION})
ENDIF()
