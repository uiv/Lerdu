cmake_minimum_required(VERSION 2.8)

PROJECT(xz)

SET(VERSION "0.1.07")

#SET(CMAKE_C_FLAGS   "-O2 -Wcpp")

include_directories(AFTER

)

SET(CURPATH "")
INCLUDE(inc.cmake)

IF(OS_CONTRIB_XZ_SRC)
	ADD_LIBRARY(xz SHARED ${OS_CONTRIB_XZ_SRC})
	SET_TARGET_PROPERTIES(xz PROPERTIES OUTPUT_NAME xz)
	SET_TARGET_PROPERTIES(xz PROPERTIES CLEAN_DIRECT_OUTPUT 1)
	SET_TARGET_PROPERTIES(xz PROPERTIES SOVERSION ${VERSION})
	ADD_LIBRARY(xz_static STATIC ${OS_CONTRIB_XZ_SRC})
	SET_TARGET_PROPERTIES(xz_static PROPERTIES OUTPUT_NAME xz)
ENDIF()
